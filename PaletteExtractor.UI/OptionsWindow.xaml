<Window x:Class="PaletteExtractor.UI.OptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PaletteExtractor.UI"
        mc:Ignorable="d"
        Title="Options" Height="140" Width="300" ResizeMode="NoResize"
        Loaded="Window_Loaded">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="50*" />
      <RowDefinition Height="50*" />
    </Grid.RowDefinitions>

    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
      <Label Name="lblOutputWidth" Content="{StaticResource strLblOutputWidth}" VerticalAlignment="Center" Grid.Row="0" Grid.Column="0" />
      <TextBox Name="txtOutputWidth" Grid.Row="0" Grid.Column="1" Height="20" Width="80" TextAlignment="Right" Margin="8" />
    </StackPanel>
    
    <Button Name="btnOk" Content="{StaticResource strBtnOk}" Click="btnOk_Click" HorizontalAlignment="Right" Grid.Row="1" Grid.Column="1" Margin="8" Width="80" />
  </Grid>
</Window>
